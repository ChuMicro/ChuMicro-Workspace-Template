# Per-project config — deep-merged over workspace.yml's `defaults:` block
# at deploy time.  Values here win at any nesting depth.
#
# Keep credentials out of git: set ``defaults.wifi.password`` (etc.) in
# the gitignored ``workspace.local.yml`` overlay rather than inlining
# them here.  The deploy-time merge picks the overlay up automatically
# as one more layer (Decision 0057).

# [wifi]
# ssid = "MyNetwork"
